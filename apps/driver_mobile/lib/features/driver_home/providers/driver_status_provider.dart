import 'package:flutter/material.dart';

class DriverStatusProvider extends StatelessWidget {
  const DriverStatusProvider({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('TODO: DriverStatusProvider')));
  }
}
