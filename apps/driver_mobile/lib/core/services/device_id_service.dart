import 'package:flutter/material.dart';

class DeviceIdService extends StatelessWidget {
  const DeviceIdService({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('TODO: DeviceIdService')));
  }
}
